import { defineConfig } from 'astro/config'; import mdx from '@astrojs/mdx'; import sitemap from '@astrojs/sitemap'; import react from '@astrojs/react'; import tailwindcss from '@tailwindcss/vite'; export default defineConfig({ site: 'https://ensinamentosdavida.com.br', cacheDir: './.astro-cache', integrations: [mdx(), sitemap({ filter: (page) => !page.includes('/404') }), react()], vite: { plugins: [tailwindcss()] } });