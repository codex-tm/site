import { glob } from 'astro/loaders'; import { defineCollection, z } from 'astro:content'; const posts = defineCollection({ loader: glob({ pattern: '**/*.md', base: './src/content/posts' }), schema: z.object({ title: z.string(), slug: z.string(), category: z.string(), excerpt: z.string(), date: z.coerce.date(), readTime: z.string().optional(), featured: z.boolean().default(false), status: z.enum(['ideia', 'rascunho', 'revisao', 'publicado']).default('publicado'), tags: z.array(z.string()).default([]), related: z.array(z.string()).default([]) }) }); export const collections = { posts };